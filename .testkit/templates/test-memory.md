# Test Memory — <feature>

> Authored by `/testkit.clarify`. This is the durable hand-off so any agent can
> pick up and run the full test execution. `tools_required` is scoped to what
> each scenario actually exercises — Discovery validates exactly this list.

## Global

- Environment:
- Auth / secrets needed:
- Shared setup:

## Scenarios

### TC-001 — <scenario name>

- **procedure** (manual steps, copy-pasteable):
  1.
  2.
- **inputs**: <concrete example values>
- **oracle**:
  - PASS when: <exact observable signal>
  - FAIL when: <signal>
- **tools_required**: <only tools this scenario invokes>
- **setup**:
- **teardown**:

<!-- repeat per scenario id -->

## Union of tools_required (sanity-check before Discovery)

-
