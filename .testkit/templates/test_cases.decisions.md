# Scenario Decision Log — <feature>

> Authored by `/testkit.scenarios`. The CSV is the machine-readable contract;
> this companion records the choices behind it that a user should be able to
> audit: why a behavior was/was not turned into a scenario, how an
> `expected_result` was made observable, and any assumption about the spec.
> Append-only — never rewrite a prior entry.

- [<TC-id or "global">] <decision> — rationale: <why>
